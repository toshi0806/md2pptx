# Python
__pycache__/
*.py[cod]

# Packaging / build artifacts
build/
dist/
*.egg-info/
.eggs/

# Claude Code local settings
.claude/settings.local.json

# Generated output (build artifacts)
example.pptx
example.pdf
base.pptx
*-slide.pptx
*.pptx.tmp
*.pdf

# Office lock/temp files
~$*

# Markdown プレビューアの書き出し（Emacs など。THEME.html のように .md の隣にできる）。
# .md はすべてルート直下にあるので、出力もそこにしか落ちない。将来サブディレクトリで
# HTML を管理したくなったときに黙って弾かないよう、ルートだけに限定する。
/*.html
