# テーマを直して見た目を変える

md2pptx は**色もフォントもレイアウトも決めません**。全部テーマファイルから持ってきます。
だから見た目を変えたいときに直すのは Markdown ではなく、**テーマの方**です。

逆に言えば、テーマさえ整えておけば、あとは中身を書くだけで同じ見た目のスライドが出てきます。

以下は **macOS の PowerPoint を前提**に書いています。Windows でもやることは同じです。
**メニュー名はバージョンや言語設定で変わることがある**ので、
名前が一致しないときは同じ意味のものを探してください（例:「現在のテーマを保存」が「テーマとして保存」になっている）。

- [どのファイルを直すのか](#どのファイルを直すのか)
- [やりたいこと別・どこを触るか](#やりたいこと別どこを触るか)
- [使う（通しの手順）](#使う通しの手順)
- [気をつけること](#気をつけること)
- [記法から色を指定する](#記法から色を指定する)
- [`.thmx` を渡された／`.thmx` で配りたい](#thmx-を渡されたthmx-で配りたい)
- [うまくいかないとき](#うまくいかないとき)

## どのファイルを直すのか

原稿の先頭（フロントマター）に書いてあるファイルです。

```yaml
---
theme: OfficeTheme.pptx      # ← これ
output: example.pptx
---
```

`--theme` を付けて実行しているならそちらが優先です。

```bash
md2pptx example.md --theme OfficeTheme.pptx -o example.pptx
```

**テーマは自分で用意するもの**で、md2pptx を**インストールしても付いてきません**。
ただしこのリポジトリには `OfficeTheme.pptx` が置いてあります。
PowerPoint の標準テーマをそのまま保存したものなので、コピーして出発点にできます。

## やりたいこと別・どこを触るか

いずれも**スライドマスター**（次の節）の中での作業です。

| やりたいこと | 触るところ |
|---|---|
| 全体の色を変えたい | 「配色」を選ぶ／作る |
| フォントを変えたい | 「フォント」を選ぶ／作る |
| ロゴを入れたい | いちばん上の**マスター**に画像を置く（全レイアウトに出る） |
| タイトルの位置・大きさ | マスターの**タイトルプレースホルダ** |
| 箇条書きの文字サイズ | マスターの**本文プレースホルダ**（レベルごとに設定できる） |
| 箇条書きの記号を変えたい | 同上。段落ごとの行頭文字の設定 |
| 図（箱・矢印）の色 | 「配色」の**アクセント1・2・6** と**テキスト2** |
| 表の見出し行の色 | 「配色」の**アクセント2** |
| スライドの縦横比 | 「スライドのサイズ」 |
| 背景 | マスターの背景書式 |

**文字の大きさを Markdown 側で決めることはできません**（`@body-size`
で*相対的に*上下はできますが、基準そのものはテーマが持ちます）。太さや色を1か所だけ変えたい、
といった用途にはテーマは向きません。

## 使う（通しの手順）

**1. テーマファイルを PowerPoint で開く**

`OfficeTheme.pptx` をダブルクリックするだけです。ふつうのプレゼンテーションとして開きます。

**2. スライドマスターを表示する**

「表示」→「スライドマスター」。

ここが**見た目の設計図**です。左側に細長い一覧が出ます。いちばん上の大きいものが
**マスター**（全体の親）で、その下にぶら下がっているのが**レイアウト**です。

- **マスター**を直すと**全レイアウトに効きます**（ロゴ・背景・既定のフォントなど）
- **レイアウト**を直すとそのレイアウトを使うスライドだけに効きます

**3. 直す**

前の表を見ながら該当箇所を変更します。配色とフォントは、
マスター表示のタブに「配色」「フォント」というボタンがあります。

自分で色を決めたいときは「配色」→「色のカスタマイズ」。
ダイアログに「アクセント1」〜「アクセント6」「テキスト/背景」が並んでいます——**この名前がそのまま
md2pptx の色名**です（[記法から色を指定する](#記法から色を指定する)）。

**4. マスター表示を閉じて保存する**

「マスター表示を閉じる」→ 保存。
**上書き保存**してください（別名にすると原稿が古い方を指したままになります）。

**5. 作り直して確かめる**

```bash
md2pptx example.md --theme OfficeTheme.pptx -o example.pptx --pdf
```

`--watch` で走らせているなら**保存しただけで作り直されます**（テーマも監視対象です）。
編集しながら確かめる方法は [VSCODE.md](VSCODE.md) にあります。

## 気をつけること

### どれを直すと、どのスライドが変わるか

マスター表示に並んでいるレイアウトは、それぞれ別の場面で使われます。
**直したいスライドがどれを使っているか**を先に確かめてください。上から4つだけが使われ、
順番も下の表のとおりです。

| レイアウト（標準テーマでの名前） | 使われるスライド | 中身の入り方 |
|---|---|---|
| 「タイトル スライド」 | 1枚目（フロントマターから作られる） | `title:` と `subtitle:` が**タイトル枠**、`author:` と `affiliation:` が**サブタイトル枠** |
| 「タイトルとコンテンツ」 | `##` で始めたスライド（いちばん多い）と、`---` で始めた見出しなしのスライド | 見出しが**タイトル枠**、本文が**本文枠**（`---` のときはタイトル枠が空のまま） |
| 「セクション見出し」 | `#` で始めたスライド（章の扉） | 見出しが**タイトル枠**。本文枠は使いません |
| 「2 つのコンテンツ」 | `<!-- @col -->` を置いたスライド | 見出しが**タイトル枠**、印より前が**左の枠**、後ろが**右の枠** |

**ふつうのスライドは `##`、章の扉が `#`** です。原稿のほとんどは `##` なので、
直していちばん効くのは「タイトルとコンテンツ」になります。

5番目より下のレイアウトは、`<!-- @layout: N -->` で名指ししない限り使われません。

### 並べ替えると、この対応が崩れる

上の表が成り立つのは、**PowerPoint の標準テーマがちょうどこの順に並んでいるから**です。
md2pptx が見ているのは**レイアウトの名前ではなく、一覧の上からの並び順**で、
1番目をタイトル、2番目をふつうのスライド、3番目を章の扉、
4番目を2カラムに使います（下に出てくる `@layout` は0から数えるので、
この1〜4番目はそれぞれ `0`〜`3` にあたります）。名前を変えても動きは変わりませんし、
逆に**中身を入れ替えると名前が合っていても入れ替わります**。

**マスターでレイアウトを並べ替えたり、上の方に新しいレイアウトを挿入すると、
全部のスライドが別のレイアウトで作られます。** 増やすなら**下に足して**ください。

特定のスライドだけ別のレイアウトを使いたいときは番号で指定できます。
**この番号だけは0から数えます**（`@layout: 3` は上から4番目）。

```markdown
<!-- @layout: 3 -->
```

### 枠は動かしてよいが、本文の枠は消さない

**位置と大きさは自由に変えられます。** タイトルを下端に、本文を左半分に置いても、
md2pptx はそのとおりに従います（表や図もその枠の中に収まります）。
ヘッダが上にある前提は持っていません。

**ただし本文の枠そのものは残してください。** md2pptx は本文の枠を、
PowerPoint がプレースホルダごとに内部で持っている**番号**で見分けています。
番号は**レイアウトごとに**振られていて、
「タイトルとコンテンツ」ならタイトルが0・本文が1、「タイトル スライド」の1はサブタイトル、
「2 つのコンテンツ」は1と2が左右の枠です（標準テーマでの実測値。**自分のテーマで違っていても、
やることは変わりません**）。**この番号は画面に出ません**——枠を選んでも、
どこにも表示されず、書き換えることもできません。だから「番号を確かめる」のではなく、
**番号が変わる操作をしない**ようにします。消して作り直すと別の番号が振られることがあり、
そうなると:

- **地の文（導入文・結論文）が出力から消えます。** 消えたことは警告に出ます（下記）が、
  生成そのものは成功扱いのまま進みます
- 表や図は、テーマの枠ではなく**上下左右に余白を取った既定の矩形**へ置かれます

```
md2pptx: warning: no body placeholder (idx 1) on this layout; dropped 2 line(s) of body text starting '導入文です。'
```

枠が要らないときも、消すのではなく**空のまま置いておく**か、位置と大きさで調整してください。

どうしても番号を確かめたいときは
python-pptx で読めます（`Presentation("theme.pptx").slide_layouts[1].placeholders` の
`placeholder_format.idx`）。ただし普通は不要で、**作り直さない**だけで足ります。

### テーマの中のスライドは出力に出ない

md2pptx はテーマを**土台**としてだけ使い、
そこにスライドが入っていても**全部捨てて0枚から**作り直します。

つまり**確認用のページをテーマに残したまま運用できます**。実際このリポジトリの
`OfficeTheme.pptx` にも1枚入っていますが、出力にその1枚は現れません。レイアウトを直したあと
PowerPoint 上でどう見えるかを、その場で確かめられます。

### スライドサイズもテーマから読む

16:9か4:3かはテーマが決めます。原稿側では指定できません。

## 記法から色を指定する

図の箱や採番記号は、既定ではテーマのアクセント色が順に割り当てられます。
**個別に指定** したいときは色名を書きます。

```markdown
<!-- @autonum-color: tx1 -->
```

書ける名前は次の10個で、
**PowerPoint の「色のカスタマイズ」ダイアログに並んでいるもの**と対応します。

| 記法で書く名前 | ダイアログの表示 |
|---|---|
| `accent1`〜`accent6` | アクセント1 〜 アクセント6 |
| `tx1` / `tx2` | テキスト1 / テキスト2 |
| `bg1` / `bg2` | 背景1 / 背景2 |

**色そのもの（`#FF0000` のような値）は書けません。**
色を変えたいならテーマの配色を変えます——そうすればテーマを差し替えるだけで全体の色が変わる、
という利点が保たれます。

## `.thmx` を渡された／`.thmx` で配りたい

`.thmx` は PowerPoint の**テーマ配布用**の形式で、md2pptx はこれも直接受け取れます。

```bash
md2pptx example.md --theme corporate.thmx -o example.pptx
```

ただし**編集するなら pptx の方が楽**です。`.thmx` は開いて直して保存、では済みません。

| | `.pptx` | `.thmx` |
|---|---|---|
| 編集 | 開く → 直す → 保存 | 適用 → 直す → **書き出し直す** |
| 確認用スライド | 置いておける | 置けない |
| 他人のデッキに適用 | しにくい | **これが本来の用途** |

**`.thmx` を渡されたときは、いったん pptx にしてから育てる**のがおすすめです。

1. **そのテーマが適用されたプレゼンテーションを1つ作る**。ふつうは `.thmx` をダブルクリックすれば、
   それが適用された新規プレゼンテーションとして開きます（関連付け次第で開かないときは、
   PowerPoint 側からテーマを読み込んでも構いません）
2. そのまま `.pptx` として保存する
3. 以降はその pptx を直す

逆に**テーマとして配りたい**ときは、
マスター表示で整えたあと「デザイン」タブのテーマ一覧から「現在のテーマを保存」で
`.thmx` に書き出せます。

なお `.thmx` は**マスターとレイアウトを含むフルテーマ**である必要があります。
配色とフォントだけの簡易テーマは、スライドの土台にできないのでエラーになります。

## うまくいかないとき

**配色を変えたのに色が変わらない**

**原稿で色名を名指ししている箇所は、テーマの配色より優先されます。**
書いていないか確認してください。

- 図の箱: `[ラベル]{accent6}`（記法は [README のフロー図](README.md#フロー図)）
- 採番記号: `<!-- @autonum-color: tx1 -->`

**文字サイズを変えたのに効かない**

本文の基準サイズは**マスターの本文プレースホルダ**が持っています。
個別のレイアウト側で上書きされていると、そちらが勝ちます。原稿側の `@body-size` や
`@autofit`、`default_autofit` が効いている可能性もあります。

**スライドが思っていたのと違うレイアウトで出る**

マスターでレイアウトを並べ替えていないか確認してください（[並べ替えると、この対応が崩れる](#並べ替えるとこの対応が崩れる)）。
`@layout: N` を書いている場合は、**0から数える**ことに注意してください。

**`.thmx` を指定したらエラーになる**

```
md2pptx: this thmx has no slideMaster (not a full theme).
```

配色・フォントだけの簡易テーマです。スライドの土台にできないので、フルテーマか
pptx を使ってください。

**地の文が出てこない／表や図がテーマの枠と違う場所に出る**

この警告が出ていないか確認してください。

```
md2pptx: warning: no body placeholder (idx 1) on this layout; dropped …
```

出ていれば、本文の枠を作り直したのが原因です（[枠は動かしてよいが、本文の枠は消さない](#枠は動かしてよいが本文の枠は消さない)）。
標準のレイアウトから位置・大きさだけを変えたものであれば起きません。

**テーマを直したのに出力が変わらない**

原稿のフロントマター `theme:` と、実際に直したファイルが同じか確認してください。
`--theme` を付けて実行している場合はそちらが優先されます。

**中で何が起きているか見たい**

`.thmx` から作られる中間ファイルを残せます。

```bash
md2pptx example.md --theme corporate.thmx -o out.pptx --keep-base base.pptx
```

`base.pptx` を開けば、md2pptx が土台にしているものをそのまま確認できます。
