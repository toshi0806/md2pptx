# Markdown 記法

md2pptx で書ける記法の一覧です。[**早見表**](#早見表)で当たりを付けて、各節へ進んでください。

コマンドラインオプション・PDF プレビュー・`--watch` は [README.md](README.md)、
見た目（配色・フォント・レイアウト）の変え方は [THEME.md](THEME.md) を参照してください。

> このファイルは**書き方**を説明します。**厳密仕様は [DESIGN.md](DESIGN.md) §5 にあります**。
> 記法を変えるときは両方を直してください（片方だけだと食い違います）。

## 早見表

**本文もこの順に並んでいます**（上から読んでも、必要な行から飛んでも同じ道筋になります）。

| 書きたいもの | 記法 | 詳しく |
|---|---|---|
| デザイン・出力先 | フロントマター（キー5つ） | [フロントマター](#フロントマター) |
| 表紙 | `# 発表の見出し` | [スライドをつくる](#スライドをつくる) |
| 章の扉 | `## 章の見出し` | [スライドをつくる](#スライドをつくる) |
| ふつうのスライド | `### スライドの見出し` | [スライドをつくる](#スライドをつくる) |
| 箇条書き | `- 項目` ／ `* 項目` | [行頭マーカー記法](#行頭マーカー記法) |
| 採番 | `1. ` ／ `① `（①〜⑳）／ `(1) ` | [行頭マーカー記法](#行頭マーカー記法) |
| 結論・補足行 | `→ 結論` | [行頭マーカー記法](#行頭マーカー記法) |
| 1行だけ大きく・小さく | `{+1}` ／ `{-2}` | [行ごとの相対フォントサイズ](#行ごとの相対フォントサイズ) |
| 強調・色・等幅・上付下付・リンク | `**強調**` ／ `[語]{red}` など | [行内装飾](#行内装飾) |
| 行内で改行 | `<br>`（タイトル・本文） | [明示改行](#明示改行) |
| 途中から大きさを変える | `<br>{-2} 続き` | [明示改行](#明示改行) |
| 表 | Markdown 標準テーブル | [表](#表) |
| フロー図 | ` ```flow ` ブロック | [フロー図](#フロー図) |
| シーケンス図 | ` ```seq ` ブロック | [シーケンス図](#シーケンス図) |
| 大きな矢印 | ` ```arrow ` ブロック ／ `<!-- @col: arrow -->` | [2カラム](#2カラム) |
| 画像 | `![alt](f.png){width=70%}` ／ ` ```image ` ブロック | [画像](#画像) |
| コード・端末セッション | ` ``` ` フェンス（等幅） | [コードブロック](#コードブロック) |
| 発表者ノート | ` ```note ` ブロック | [発表者ノート](#発表者ノート) |
| 2カラム | `<!-- @col -->` | [2カラム](#2カラム) |
| 段階表示（アニメーションの代わり） | `<!-- @step -->` | [段階表示](#段階表示) |
| スライド単位の上書き | `<!-- @layout: 5 -->` など全9種 | [ディレクティブ](#ディレクティブ) |

## フロントマター

ファイル冒頭を `---` で囲み、テーマと出力先を書きます。
**`---`…`---` の内側は YAML、それ以降は Markdown 本文**として解釈されます。

```yaml
---
theme: OfficeTheme.pptx   # デザインのおおもと（.thmx / .pptx）
output: out.pptx
slide_number: true        # 表紙以外に番号を付与（既定 true）
default_autofit: true     # 本文があふれる場合に縮小（既定 true）
syntax: 1                 # 見出しレベルの割り当て（既定 1。省略できます）
mono_font: Consolas       # コードブロックの等幅フォント（既定 Consolas）
---
```

使えるキーはこの6つだけです。**未知のキーはタイポとみなしてエラーで停止**します。

`syntax` は見出しレベルの割り当てを選びます。既定は 1 なので**書かなくて構いません**。
`0` は md2pptx 1.1 以前の割り当てで、当時の原稿をそのまま使うためのものです。

表紙も本文に書きます（→ [スライドをつくる](#スライドをつくる)）。

## スライドをつくる

**見出しを書くと、そこから新しいスライドが始まります。**

- `# 見出し`（H1）は**表紙**（→ [表紙](#表紙)）。デッキに1枚だけです
- `## 見出し`（H2）はセクション見出しスライド（章の扉）。**本文も書けます**
- `### 見出し`（H3）はふつうのスライド（見出し＝タイトル）。**原稿のほとんどはこれ**です
- `####` 以降（H4〜H6）は**未定義でエラー**（将来のスライド内小見出し用に予約）

見出しの深さが文書の階層と一致します——表紙が最上位、章が次、スライドがその下です。

### 表紙

`# 見出し` がそのまま表紙になり、テーマの「タイトル スライド」レイアウトで
描かれます。**表紙には番号が付きません。** 2枚目を書くとエラーになります。

```markdown
# ソフトウェア開発手法を応用した<br>LaTeX論文執筆環境の構築<br>{-3} ― 副題 ―

- 下川 俊彦
- 九州産業大学 理工学部 情報科学科
- {-2}
- {-2} 情報処理学会 インターネットと運用技術研究会(IOT74)
- {-2} 2026年7月
```

- 主題・副題はタイトル枠に入ります。`<br>` で折り、`{-3}` のように段数を添えると
  **その位置から先だけ**小さくなります（→ [明示改行](#明示改行)）。
  副題をタイトル枠の中に置くのはテーマの意図に沿った形です
  （多くのテーマはタイトル枠と著者欄の間に罫線を引きます）
- 著者・所属は箇条書きで書きます（記号が出ない理由は下記）
- マーカーだけの行（`- `）で1行空けられます（→ [行頭マーカー記法](#行頭マーカー記法)）
- 発表者ノートはこのスライドの中に ` ```note ` ブロックで書きます

### 書いたものがどこへ入るか

**どのスライドでも同じです。** 見出しはタイトル枠、続けて書いた行は本文枠、
表・フロー図・画像も本文の領域に置かれます。章の扉（`#`）も例外ではありません。

```markdown
# 第1部　背景と課題

- 扉にも添え書きを書けます
```

見え方はテーマが決めます。**記号が出るかどうかもテーマがレイアウトごとに決めていて**、
多くのテーマは表紙と章の扉の本文枠に記号を出さない設定にしています
（そこでは `-` で書いても記号が付きません）。md2pptx は記号を指定せずテーマに任せます。

本文枠を持たないレイアウトもあります（「白紙」「タイトルのみ」など）。
そこに地の文を書くと**置き場所が無いので捨てられ、警告が出ます**。
表・フロー図・画像は座標で置かれるので、本文枠が無くても使えます。
図や表だけのスライドは `<!-- @layout: 6 -->`（白紙）で作れます。

どのレイアウトがどんな見た目になるかは
[THEME.md](THEME.md#どれを直すとどのスライドが変わるか) にまとめてあります。

## 行頭マーカー記法

インデント（半角スペース2つ＝1レベル）でネストを表します。

| 書き方 | 表示 |
|---|---|
| `- テキスト` / `* テキスト` | 箇条書き（テーマ既定の記号） |
| `- `（マーカーだけ） | 空行（1行空ける） |
| `1. テキスト` | 自動採番 `1. 2. 3.` |
| `① テキスト` | 丸数字採番 `① ② ③`（番号文字は除去し自動採番に変換） |
| `(1) テキスト` | 丸括弧採番 `(1) (2)` |
| `→ テキスト` | 行頭記号なしの結論・補足行（`→` は表示に残る） |

- **番号はリストの先頭の数字から始まります。** 続きを別のカラムや別のスライドに
  分けても、先頭に `8.` と書けば 8 から続きます（→ [採番の続き](#採番の続き)）。
- 丸数字マーカーは `①`（U+2460）〜`⑳`（U+2473）を認識します。
  他のマーカーと違い直後のスペースは省略できます（`①本文` も可）。
- `→` 行は「`→` ＋スペース1つ＋本文」へ正規化されます。
- **箇条書きマーカーだけの行は空行になります。** 記号の出ない枠
  （タイトルスライドの著者欄、セクション見出しスライド）で、行の塊を分けるのに使えます。
  `- {-2}` と書けば空行の高さも周りに揃います。末尾の空白は有っても無くても同じです。
  採番マーカー（`①` / `(1)` / `1.`）だけの行は空行になりません
  （番号を消費するだけになるため）。

```markdown
- 下川 俊彦
- 九州産業大学 理工学部 情報科学科
- {-2}
- {-2} 情報処理学会 インターネットと運用技術研究会(IOT74)
```

採番行の直下にネストした `-` を置くと「見出し＋説明」になります。

```markdown
1. parser.py
   - Markdown を中間表現（IR）へ変換
2. render.py
   - IR を pptx に描画
```

### 採番の続き

**効くのはリストの先頭の数字だけ**で、以降は 1 ずつ増えます。
`1. 1. 1.` と書いても `1. 2. 3.` になります（標準 Markdown と同じ規則）。

長いリストを2カラムに割るときは、右カラムの先頭に続きの番号を書きます。

```markdown
### シラバス

1. コンピュータネットワークとインターネット
2. ネットワークコミュニケーション

<!-- @col -->

8. アプリケーション層(1)
9. アプリケーション層(2)
```

→ 右カラムが `8.` `9.` と続きます（何も書かなければ `1.` に戻ります）

- `①` / `(1)` でも同じです（`⑤` から始めれば 5、`(3)` から始めれば 3）。
- **数え直しは起きません。** 1つの枠の中では「深さと形式の組ごとに1本の連番」です。
  入れ子のリストは外側の項目が変わっても続きますし、地の文をはさんで
  別のリストを書いても続きから数えます（標準 Markdown とはここが違います）。
  途中から始めたいときは、その先頭に始めたい番号を書いてください。

### 行ごとの相対フォントサイズ

特定の行だけ強調・縮小したいときは、マーカー直後・本文直前に `{+1}` / `{-2}` を置きます。
**絶対 pt は指定できません**。テーマ既定サイズからの相対段数（1段 ≒ ±12.5%）だけを指定でき、
テーマを差し替えても比率が保たれます。

```markdown
- {+1} ここだけ一段大きく強調
- 通常の行
- {-2} 注記は二段小さく
① {+1} 採番行も大きくできる
→ {-1} 結論行も小さくできる
```

- 全行種（`-` / `1.` / `①` / `(1)` / `→`）で使えます。符号は省略可（`{2}` ＝ `{+2}`）。
- **`<br>` の直後にも置けます。** その位置から先だけ大きさが変わります
  （→ [明示改行](#明示改行)）。段数はどこに書いてもテーマ既定からの相対で、
  行頭の指定から数え直しません（`- {+1} 大きい行<br>{-2} 続き` の `{-2}` は
  「テーマ既定の2段下」のままです）。
- スライド全体を一律で調整するには `<!-- @body-size: -1 -->`（→ [ディレクティブ](#ディレクティブ)）。
  行の `{…}` が優先されます。`{0}` はスライドの `@body-size`
  を打ち消して「その行だけテーマ既定へ戻す」意味です（`@body-size: 0` は未指定と同義）。
- 見栄えはテーマに委ねる方針のため、これは“逃げ道”。
  常用せず強調したい一点に絞るのが推奨です。

### 行を枠で囲む

`{box}` を同じ位置（マーカー直後・本文直前）に置くと、**その段落を枠で囲みます**。
シラバスや講義日程で「今日はここ」を示すのに使います。

```markdown
1. {box} コンピュータネットワークとインターネット
2. ネットワークコミュニケーション
```

- 囲むのは**段落**です。2行に折り返しても枠は 1 つで、行内装飾（run 単位）とは別物。
- 色は `{box:blue}` のように書けます。省略するとテーマのアクセント色で、
  色名の語彙は[行内装飾](#行内装飾)と同じ（テーマ色名／CSS の色名／16進）。
  **綴りを間違えるとエラーで止まります。**
- 相対サイズと併記できます。順序は問いません（`{box}{-1}` も `{-1}{box}` も同じ）。
  同じトークンを 2 回書いたときは**後に書いたほうが残ります**。
- 全行種（`-` / `1.` / `①` / `(1)` / `→`）と、2カラム・表や図と同居するスライドでも使えます。

**枠の位置は見積もりです。** pptx を書く側からは PowerPoint が実際にどこへ行を置いたか
分からないので、「本文サイズ × 1.32」の行を積んで求めています。
`@autofit` で縮小が掛かる枠や、行間をテーマが変えている枠では**枠だけがずれる**ことが
あります（文字は動きません）。ずれたら `{box}` を外すか、行を短くしてください。

## 行内装飾

行の一部だけ見た目を変えられます。**本文行だけ**で、タイトルや表のセルでは効きません。
図の `note(top):` / `note(bottom):` は本文へ流れる地の文なので効きます
（図の中のラベルや `caption:` では効きません）。

| 記法 | 表示 |
|---|---|
| `**強調**` | 太字 |
| `` `等幅` `` | 等幅（コマンド名・プロトコル名など） |
| `[語]{red}` | 文字色 |
| `2^32^` ／ `H~2~O` | 上付き ／ 下付き |
| `[表示文字](url)` | ハイパーリンク |

```markdown
- IPv4 のアドレス空間は 2^32^ 個、**約43億**
- `dig` で名前解決を確かめる（参考: [JPNIC](https://www.nic.ad.jp/)）

→ NAT では [192.168.0.2]{blue} が [203.0.113.1]{red} に書き換わる
```

- **重ねられます**。`[**赤い強調**]{red}` のように書けます。
- `[語]{色}` と `[表示](url)` は括弧の種類で区別します。
- **上付き・下付きの中に空白は使えません**（`2^32^` は効きますが `2^3 + 1^` は
  そのまま文字として出ます）。短い添字のための記法です。
- 対になる記号が無ければ、そのまま文字として出ます（`a**b` は `a**b` のまま）。

### 色の指定

**3系統**を受け付けます。

| 系統 | 例 | 使いどころ |
|---|---|---|
| テーマ色名 | `accent1`〜`accent6` / `tx1` / `tx2` / `bg1` / `bg2` | **既定はこちら。** テーマを差し替えても配色が破綻しません |
| 具体的な色名 | `red` / `blue` / `green` / `crimson` … | 「危険＝赤」「正常＝緑」のように**意味が色そのものに宿る**とき |
| 16進 | `#ff0000` / `#f00` | 具体的な色名に無い色 |

- 具体的な色名は **CSS の基本色名**（148色）をそのまま使えます。大小文字は問いません。
- **綴りを間違えるとエラーで止まります。** 黙って既定色にはしません
  （色が付いていないことに気づかないまま配ることになるからです）。
- 同じ色名は `<!-- @autonum-color: red -->` でも使えます。
  フロー図の `[名前]{accent6}` は現在テーマ色名だけです。

## 明示改行

`<br>`（`<br/>` も可）は**タイトルにも本文行にも同じように書けます**。その位置で改行します。

```markdown
### 行頭マーカー記法：<br>見た目で段落の種別を判定

- 長い項目はここで折り返す<br>続きはぶら下げ位置に揃います
```

`<br>` の直後には相対サイズトークンを置けます。**その位置から先だけ**大きさが変わり、
段落は分かれません。タイトル枠の中に副題を収めるときに使います。

```markdown
# ソフトウェア開発手法を応用した<br>LaTeX論文執筆環境の構築<br>{-2} ― DevContainer・CI・PRレビューの統合 ―
```

タイトルは先頭にも置けます（`### {+1} 大きい見出し<br>{-1} 小さい続き`）。
本文行の先頭は行全体への指定で、ビュレットや採番記号も一緒に変わります。

タイトルは**枠幅で自動縮小しません**。
枠に入らない行はその場で折り返され、語の途中で切れます。
折る位置は `<br>` で著者が決めてください。

分けるのは**段落の中の行**なので、箇条書きの記号や採番は付き直しません。
続きの行は**ぶら下げ位置に揃います**。段落そのものを分けたいときは行を分けてください。

## 表

Markdown 標準のテーブル記法。1行目をヘッダとしてアクセント色で着色します。

```markdown
### 課題の整理とアプローチ
<!-- @table-widths: 45,55 -->

体裁はテーマに、内容は Markdown に分離する

| 課題 | md2pptx のアプローチ |
|:--|--:|
| デザインの一貫性 | PowerPoint テーマに委譲 |
| 記述のしやすさ | Markdown の行頭マーカー記法 |

→ 色やフォントはコードに持たず、テーマに委ねる
```

- 導入文・結論文（地の文）はそのまま本文として置かれ、表は**その間に空く領域**に置かれます。
  以降この領域を**帯**と呼びます。地の文が片側だけなら残り全体、
  どちらも無ければ本文領域いっぱいを使います。
- `<!-- @table-widths: 45,55 -->` で列幅の比率を指定できます（既定は均等）。
- 区切り行のコロンで列ごとの水平寄せを指定できます：`:--:` 中央
  / `--:` 右 / `:--` 左（コロン無し `---` も左）。数値列の右寄せに便利です。
- **セルを個別に塗れます。** セルの末尾に `{色}` を書きます（→ [色の指定](#色の指定)）。

  ```markdown
  | 方向 | 変換前 | 変換後 |
  |:--|:--|:--|
  | 送信 | 192.168.0.2 {bg2} | 203.0.113.1 {accent2} |
  ```

  ヘッダのセルにも書けます（既定のアクセント色を上書きします）。
  色として解釈できない `{…}` は文字のまま残るので、`{n} 個` のような書き方は壊れません。
- 帯に収まらない表は、本文のフォントサイズを段階的に小さくして収めます。
  いちばん小さくしても収まらない場合は警告を出します（そのまま帯を超過しえます）。
  縮小せず標準サイズのまま下へはみ出したいときは `<!-- @overflow: true -->`
  （→ [はみ出しの許可](#はみ出しの許可)）。

## フロー図

` ```flow ` フェンスブロックで box・矢印のフロー図を宣言的に記述します。

````markdown
### 生成パイプライン

```flow
direction: lr
note(top): テーマと Markdown を入力に pptx を生成
[theme.thmx | テーマ]
-変換-> [base.pptx | 土台]
-描画-> [out.pptx | スライド]
-> [… | ]
caption: 配色・フォントはテーマ、内容は Markdown
note(bottom): → テーマを差し替えるだけで見た目が一新できる
```
````

| 要素 | 記法 |
|---|---|
| ノード | `[ラベル \| サブラベル]`（サブラベル省略可） |
| 省略記号 | `[…]` または `[...]`（box ではなく中央寄せの注記） |
| 矢印 | `->` / ラベル付き `-PR->` |
| 方向 | `direction: lr`（左→右、既定）/ `tb`（上→下）。それ以外の値はエラー |
| 図のキャプション | `caption: …`（box の直下） |
| 上下の地の文 | `note(top): …` / `note(bottom): …`（本文プレースホルダへ） |
| 色の個別指定 | `[名前 \| サブ]{accent6}`（色名は下記のテーマ色名） |

`note(top):` / `note(bottom):` は**本文行と同じ**扱いです。`→` で始めれば行頭記号なし、
それ以外は箇条書きになり、[行内装飾](#行内装飾)（`**強調**` / `[語]{red}` / リンク）も効きます。

ノード・矢印・設定行のいずれにも解釈できない記述はタイポとみなしてエラーで停止します。
色名に使えるのはテーマ色名 `accent1`〜`accent6` / `tx1` / `tx2` / `bg1` / `bg2`
です（`@autonum-color` と共通。無指定時はテーマから自動割り当て）。

### 名前を付けて任意のノード間を結ぶ

`[#pc PC]` のように `#` で名前を付けると、**隣り合っていないノード同士**を結べます。
分岐・合流はこれで書きます。

````markdown
```flow
[#pc PC | 192.168.0.2] -> [#nat NATルータ] -> [#srv Server | 203.0.113.1]
--
[#pc2 PC | 192.168.0.3]
pc2 -> nat
```
````

- **`--` だけの行で段が変わります。** 段の中は左から右、段は上から下に積まれます。
  列は全段で揃うので、要素数の違う段があっても格子に見えます。
- 名前は `#` で書きます。`[id: ラベル]` だと
  `[HTTP: ハイパーテキスト転送プロトコル]` のような**ラベル中のコロンと区別できない**ためです。
- 知らない名前・重複した名前はエラーで停止します。
- **段をまたぐ矢印は細い線**で、同じ段で隣り合う矢印は従来どおり塗り矢印で描かれます
  （塗り矢印はすき間に収まる形なので、離れた2点を結ぶと箱に食い込みます）。
- 矢印を続けて書いても**段はまたぎません**。段をまたぐ線は名前で明示してください。

自動レイアウト（力学モデル等）・曲線・自己ループ・ノードの絶対座標指定はありません。
格子と直線までです。

## シーケンス図

` ```seq ` フェンスブロックで、プロトコルのやりとり（ラダー図）を書きます。
時間は**上から下**へ流れます。

````markdown
### TCP 3-way handshake

```seq
lifelines: クライアント, サーバ
note(top): コネクションの確立
クライアント -> サーバ: SYN seq=x
サーバ -> クライアント: SYN+ACK seq=y ack=x+1
クライアント -> サーバ: ACK ack=y+1
caption: 3回のやりとりで確立する
note(bottom): → 以後 seq/ack で順序と到達を保証
```
````

| 要素 | 記法 |
|---|---|
| 登場人物 | `lifelines: A, B, C`（左から右の並び順） |
| やりとり | `A -> B: ラベル`（ラベルは省略可） |
| 図の中の注記 | `note: …`（その位置の右端に置かれます） |
| 図のキャプション | `caption: …` |
| 上下の地の文 | `note(top): …` / `note(bottom): …`（本文プレースホルダへ） |

- **`note(top)` / `note(bottom)` はフロー図と同じ**です。本文行と同じ扱いで、
  `→` の行頭記号なしも[行内装飾](#行内装飾)も効きます。
  図の中に置く `note:` とは別物です（あちらは図の一部）。
- **`lifelines:` は省略できます。** 書かなければ、出てきた順に登場人物ができます。
  書いた場合は、そこに無い名前はタイポとみなしてエラーになります。
- ラベル中のコロンはラベルの一部です（`ack 1: seq 100` と書けます）。
- **本数が増えても帯からはみ出しません。** 間隔を詰めて収めます
  （Fast Retransmission のように矢印が25本を超えても1枚に収まります）。
- 自分から自分への矢印・`alt` / `loop` などの制御構造・activation box はありません。
  往復の線とラベルだけです。

## 画像

画像（jpg / png）は表・フロー図と同じ扱いで、
導入文・結論文と一緒に1枚のスライドに置けます。記法は2形式あります。

**ショートハンド（標準 Markdown 画像＋末尾 `{...}`）**

```markdown
![実験結果の比較](results.png){width=70%}
![](diagram.jpg){width=8cm align=left}
```

**フェンス ` ```image `（オプションが多いとき）**

````markdown
```image
src: results.png
width: 70%
crop: 100,50,800,400
align: center
caption: 実験結果の比較
```
````

| キー | 意味 |
|---|---|
| `src` | 画像ファイル（Markdown からの相対パス）。必須 |
| `width` / `height` | 埋め込みサイズ。`70%`（帯幅比）/ `8cm` / `300pt` / `2in`（`2"` も可）/ `150px` / 単位なし＝px。省略時はアスペクト自動 |
| `crop` | トリミング。**残す矩形 `x,y,w,h`**（左上原点）。既定はピクセル、各値に `%` を付けると割合 |
| `align` | 水平寄せ `left` / `center`（既定）/ `right` |
| `fit` | `width` と `height` を両方指定したとき `contain`（既定・比維持）/ `fill`（歪ませ充填） |
| `caption` | 図下キャプション（ショートハンドは alt テキストを採用） |
| `overflow` | `true` で帯からのはみ出しを許可（`width`/`height` の明示指定が必須・無指定はエラー。はみ出すのは下方向のみ）。未指定時はスライドの `@overflow` に従い、指定時はそちらより優先 |

### 図だけのスライド

タイトルも本文も要らないときは「白紙」レイアウトを選びます。

````markdown
### TCP/IP プロトコル群
<!-- @layout: 6 -->

```image
src: figs/tcpip-stack.png
width: 100%
```
````

- 見出しは書きますが（原稿の目印として要る）、白紙レイアウトには
  タイトルの枠が無いので**表示されません**。
- **タイトルの無いレイアウトでは、上の余白をタイトルぶん空けません。**
  図がスライドいっぱいに置けます（「タイトルのみ」レイアウトは
  タイトルがあるので従来どおり空けます）。
- レイアウト番号はテーマ次第です。標準的な並びなら `5` がタイトルのみ、`6` が白紙。

- 画像パスは Markdown ファイルの置き場を基準に解決します。
  見つからない場合はエラーで停止します。
- 上記以外のキーはタイポとみなしてエラーで停止します。
- ショートハンドの `{...}` は空白区切りのため、空白を含むキャプションは
  alt テキスト（`![実験 結果](...)`）かフェンス記法の `caption:` を使ってください。
- `crop=100,50,800,400` は「左上 (100,50) から 幅800×高400 px
  の範囲を残す」の意味です（`crop=10%,5%,50%,40%` のように `%` でも指定できます）。
- 画像は自動的にアスペクト比を保ち、帯（中央領域）からはみ出さないよう収まります。
  `overflow: true` を指定すると明示した `width`/`height` を優先し、
  帯の下（結論文・罫線側）へのはみ出しを許可します（例: `height: 115%` で帯高の115%。
  上方向のタイトル・導入文には重なりません。`width`/`height` のどちらも無い場合はエラー、
  スライド下端を超える場合は警告を出力します）。
  指定サイズが帯に収まる場合（例: `height: 80%`）は通常配置と同じで、
  はみ出しは発生しません。

## コードブロック

` ``` ` で囲んだ行は**等幅フォントで、書いたまま**出ます。
端末のセッション、設定ファイル、2進数の並びなど「桁を揃えて見せたい塊」に使います。

````markdown
### HTTP: telnet で試す

Web サーバに直接リクエストを送ってみる

```console
$ telnet www.example.com 80
GET / HTTP/1.1
Host: www.example.com
```

→ 応答が返る
````

- **行は書いたまま**です。中の `- ` や `1. ` は箇条書き・採番になりませんし、
  `{+1}` も `<br>` も文字として出ます。字下げと途中の空行も残ります。
- **info string（` ```console ` の `console`）は自由**で、md2pptx は読み飛ばします。
  **構文強調はしません**（色はテーマに委ねる方針のため）。
- ` ```flow ` / ` ```image ` / ` ```note ` は従来どおりそれぞれの意味で、コードにはなりません。
- **閉じ忘れると行番号付きのエラー**で止まります。
- 等幅フォントはフロントマターの `mono_font:` で変えられます（既定は `Consolas`）。
  指定するのは欧文フォントで、日本語はテーマのフォントで描かれます。

### 長い行の収め方

**行は本文枠の幅で折り返します**（タイトルと同じく、幅に合わせた自動縮小はしません）。
桁を揃えたい図は、折り返すと読めなくなります。次の2つで収めてください。

````markdown
### 可変長サブネットマスク
<!-- @body-size: -3 -->
<!-- @widths: 108! -->

```text
IP address    192.168.1.130 = 11000000 10101000 00000001 10000010
netmask  /26                = 11111111 11111111 11111111 11000000
                              |------- network -------|--- host -|
```
````

- `<!-- @body-size: -3 -->` で本文を小さく（→ [ディレクティブ](#ディレクティブ)）
- `<!-- @widths: 108! -->` で枠を広く（→ [ディレクティブ](#ディレクティブ)）

**日本語と半角文字を1行に混ぜると桁は揃いません**（日本語はテーマのフォントで
描かれるため）。桁を揃えたい図はラベルも半角で書いてください。

## 発表者ノート

` ```note ` ブロックの中身が、そのスライドの発表者ノート（PowerPoint
の発表者ビューやノート印刷で見えるテキスト）になります。スライド面には表示されません。
オーラル原稿をスライドソースと同じファイルで版管理する用途を想定しています。

````markdown
### 背景：LaTeX論文執筆が抱える課題

- 学生側の障壁
  - OS差・TeX環境・パッケージ依存で環境構築が困難

```note
まず背景です．LaTeX の執筆環境は OS 差で環境構築が難しく，
指導のフィードバックも属人化しがちでした．
```
````

- スライド内ならどこに書いても構いません（読みやすさの点では末尾を推奨）。
  複数書くと改行で連結されます。ブロック内の改行は段落区切りとして保持されます。
- 表紙のノートも同じで、表紙スライドの中に書きます（→ [表紙](#表紙)）。

## 2カラム

`<!-- @col -->` を1つ置くと、その前後が左右2カラムに分かれ、
自動的に「2 つのコンテンツ」レイアウトになります。

```markdown
### 比較：手作業 と md2pptx

- 手作業でスライド作成
  - 体裁の調整に時間がかかる
  - デザインがばらつきやすい

<!-- @col -->

- md2pptx で生成
  - 内容（Markdown）に集中できる
  - テーマで見た目を統一
```

各カラムには箇条書き・採番だけでなく、**表・フロー図・画像**も置けます。
地の文（箇条書き）と表・図を同じカラムに混在させても重ならないよう配置され、
カラムに表・図しか無ければカラム全体に配置されます。左に要点、右に縦長の推移表、
といったレイアウトに使えます。

### 流れを大きな矢印で見せる

「左の列 → 右の列 → その下の結論」を矢印の図形で示せます。

````markdown
- 手紙（文章）
- 電話（音声）

<!-- @col: arrow -->

- 郵便配達網
- 電話網

```arrow
direction: down
```

→ 全て[アナログ]{blue}情報
````

| 書き方 | 出るもの |
|---|---|
| `<!-- @col: arrow -->` | カラムの区切りに右向きの大きな矢印 |
| ` ```arrow ` ブロック | 本文の中に置く大きな矢印 |

````markdown
```arrow
direction: down
```
````

- `direction:` は**必須**。`down` / `up` / `right` / `left` / `updown` / `leftright`。
  知らないキー・知らない向きはエラーで止まります。
- 表や図と同じ**ブロック**なので、置いた位置（導入文と結論文の間）にそのまま入ります。
  1枚に2つ以上置けます。
- `@col: arrow` の矢印は**左カラムの右寄り**に置かれます。テーマのカラム間のすき間は
  0.5cm ほどしか無く、そこへ収めると矢印が糸のように細くなるためです。
  **左カラムの箇条書きが長いと文字に重なります**——短い項目を並べる版で使ってください。
- `arrow` 以外の値はエラーで止まります。

## 段階表示

md2pptx はアニメーションを書きません。代わりに `<!-- @step -->` を置くと、
**そこまでの内容で1枚**を作り、続きを積み上げていきます。

```markdown
### TCP 3-way handshake

- SYN を送る
<!-- @step -->
- SYN+ACK が返る
<!-- @step -->
- ACK を返して確立
```

3枚できます。1枚目は1行、2枚目は2行、3枚目は3行です。

- **積み上がります。** 消えて次が出るのではなく、既に見せたものはそのまま残ります。
- **タイトル・レイアウト・ディレクティブは全段で同じ**です。`@step` より後に書いた
  `<!-- @layout: 5 -->` も全段に効きます（段はスライドの一部であって別のスライドではないため）。
- **発表者ノートは最終段だけ**に付きます。慣例どおり ` ```note ` をスライド末尾に
  書けばそうなります。発表者ビューに同じ原稿が何度も出ても読みにくいだけだからです。
- 積み上がるのは箇条書きだけではありません。**表・フロー図・画像**も同じです。
- 本文より先に置けば、タイトルだけの段から始められます。
- **スライド番号は段ごとに進みます。**

### 図の中で段階を切る

フェンスの中に `@step` の行を書くと、**図だけがその段階の姿**になります。
地の文は従来どおり積み上がります。

````markdown
### TCP 3-way handshake

クライアントとサーバが3回のやりとりで接続を確立する

```seq
lifelines: クライアント, サーバ
クライアント -> サーバ: SYN seq=x
@step
サーバ -> クライアント: SYN+ACK seq=y ack=x+1
@step
クライアント -> サーバ: ACK ack=y+1
```

→ 以後 seq/ack で順序と到達を保証
````

- ` ```seq ` は**矢印1本**ずつ、` ```flow ` は**ノード1個**ずつ増えます。
- **登場人物は最初から全員います**（途中で増えると図が横に動いて読みにくいため）。
- **矢印は、その先のノードが出た段で一緒に出ます**（片端しか無い線を描きません）。
- 図の中の注記（`note:`）は、その位置の矢印が出た**次の段**から現れます。

多カラムでも使えます。段はどれも**最終的なカラム構成**で描かれるので、
カラム区切りより前の段は右カラムが空の状態になります
（途中の段だけ単一カラムにすると、行頭の位置が段ごとに動いてしまうためです）。

```markdown
### 比較：手作業 と md2pptx

- 手作業でスライド作成
<!-- @step -->
  - 体裁の調整に時間がかかる

<!-- @col -->

- md2pptx で生成
<!-- @step -->
  - 内容に集中できる
```

## ディレクティブ

行頭マーカーで表せない指定は、スライド先頭の HTML コメントで補います。

| ディレクティブ | 効果 |
|---|---|
| `<!-- @autonum-color: tx1 -->` | このスライドの採番記号色を指定（テーマ色名 `accent1`〜`accent6` / `tx1` / `tx2` / `bg1` / `bg2`） |
| `<!-- @layout: 5 -->` | レイアウト番号を上書き（**0から数える**。`5` はマスターの上から6番目） |
| `<!-- @autofit: 90 -->` | 本文を90% に縮小して焼き込む |
| `<!-- @body-size: -1 -->` | このスライドの本文を一律で相対調整（行の `{+1}` が優先） |
| `<!-- @table-widths: 45,55 -->` | 表の列幅比率（多カラムではスライド内の全カラムの表に共通適用） |
| `<!-- @col -->` | カラム区切り（「2 つのコンテンツ」レイアウトになる）。値は `arrow` のみ |
| `<!-- @step -->` | 段階の区切り（→ [段階表示](#段階表示)）。値は取らない |
| `<!-- @widths: 55,45 -->` | プレースホルダ幅（標準の使用可能幅に対する%）。値1個＝単カラム本文幅（例: `104`）、複数＝カラムごとの幅。合計>100で全体が拡幅 |
| `<!-- @overflow: true -->` | このスライドの表・画像に、帯（中央領域）から下方向へのはみ出しを許可（→ [はみ出しの許可](#はみ出しの許可)） |

- **未知のディレクティブはタイポとみなしてエラーで停止**します（行番号付き）。
- `@widths` の拡幅は**左端固定・右方向のみ**が既定（行頭位置がスライド間で揃う）。
  右余白で収まらない指定はクランプ＋警告になります。値の末尾に `!` を付ける（例: `108!`
  / `62,47!`）と、収まらない分だけ左余白へ逃がすことを許可します（スライド端は余白
  0.1in でクランプ）。
- 値の区切り・記号は全角（`，` `％` `！`）でも受理します。
  キー名のハイフンはアンダースコアと同一視します（`@body-size` ＝ `@body_size`）。

### はみ出しの許可

`<!-- @overflow: true -->` を置くと、
そのスライドの**表・画像**は「帯（中央領域）に収める」制約を外せます。共通の規約:

- 値は `true` / `false` のみ。それ以外はエラーで停止します。
- **上端は帯の上端に固定**され、はみ出すのは**下（結論文・罫線側）のみ**。
  タイトル・導入文には重なりません。
- **表**: フォントを段階縮小せず、**本文標準（lvl1）サイズのまま**必要な高さで描画します。
  行数の多い表が読めないほど小さくなるのを防げます。
- **画像**: スライド既定として作用します。画像ブロックの
  `overflow:`（per-block）を明示した場合は**そちらが優先**されます。
  サイズ（`width`/`height`）の明示がない画像は帯に内接するため、
  `@overflow` を置いても変化しません。
- 帯に収まる表・画像には効果がありません（通常配置と同じ）。
- **結論文との重なりは仕様**です（結論文の帯に意図的に重ねる機能）。
  実際に重なる場合とスライド下端を超える場合は stderr に警告を出します。
- フロー図は対象外です（帯に内接固定。将来拡張）。
